# Notebooks

This folder contains the exploratory notebooks used to prototype the model
ideas that were later turned into scripts in `projects/`.

Recommended workflow

1. Use notebooks for early experiments and visualization.
2. Move reusable logic into `projects/` once it becomes stable.
3. Keep notebook outputs small and avoid committing large generated artifacts.
